doctype html
head
  title Sign Up
  meta(charset='UTF-8')
  meta(name='viewport' content='width=device-width, initial-scale=1')
  style.
    .header{
    padding: 20px;
    text-align: center;
    background: #FFFFFF;
    }
    .navigationBar {
    overflow: hidden;
    background-color: #404E6C;
    }
    .navigationBar a {
    /*Style for the Tab Text in Navigation Bar*/
    font-family: sans-serif;
    font-style: normal;
    font-weight: bold;
    color: white;
    float: left;
    text-align: center;
    padding: 14px 16px;
    text-decoration: none;
    font-size: 17px;
    }
    .navigationBar a.positionRight {
    /*Positions the Signout Button Correctly*/
    float: right;
    }
    * {
    /* Ensures that the main column is next to side column */
    box-sizing: border-box;
    }
    /* Column container */
    .row {
    display: flex;
    flex-wrap: wrap;
    }
    .sideColumn {
    flex: 20%;
    background-color: #f1f1f1;
    padding: 20px;
    }
    .mainColumn {
    flex: 80%;
    background-color: white;
    padding: 20px 40px;
    }

    p{
      font-family: sans-serif;
    }
    button{
      font-family: sans-serif;
      background-color: #0033A1; 
      border: none;
      color: white;
      padding: 13px 23px;
      font-size: 12px;
      font-weight: bold;
      border-radius: 6px;
    }
    button:hover {
    background-color: #012573;
    color: white;
    }
    .card {
     box-shadow: 0 4px 8px 0 rgba(0,0,0,0.2);
     transition: 0.3s;
     width: 40%;
     display:inline-block;
     border-radius: 5px;
     background-color: white;
    }
    .card:hover {
     box-shadow: 0 8px 16px 0 rgba(0,0,0,0.2);
    }
    h1 {
        font-family: sans-serif;
        text-align: center;
        font-size: 25px;
        color: #0033A1;
    }
    input[type=text], select {
    width: 100%;
    padding: 12px 20px;
    margin: 8px 0;
    display: inline-block;
    border: 1px solid #ccc;
    border-radius: 4px;
    box-sizing: border-box;
    }
    label{
      font-family: sans-serif;
      font-size: 13px;
      font-weight: bold;
    }
.header
  img(src='logo.png' width='40%' height='40%')
.navigationBar
  a.positionLeft(href='/home') Homepage
  a.positionLeft(href='/search') Search
  a.positionLeft(href='/create') Create
  a.positionRight(href='/logout') Log Out
  a.positionRight(href='/profile') Profile
.row
  .mainColumn(style="background-color: #F5F8FA")
    .trendingMovies
      div(class='card' style="margin-left: 20px; margin-top: 10px")
        table
            tr
              th
            tr
              td
                div(class='container' style='padding: 18px 20px 20px 20px')
                  h1
                    | Create a Director/Writer/Actor
                  form(action="/users" method="POST")
                    label NAME* 
                      input(type='text' name="name" placeholder="ex: Tom Cruise", required)
                      br
                      br
                    label ROLE*
                      input(type='text' name="role" placeholder="ex: Actor/Director/Writer", required)
                      br
                      br
                    button(type='submit') Create Person
